## Environment setup
- `uv sync`
- `source .venv/bin/activate`

## Running python scripts
- `uv run script.py`

## Development Guidelines
- Ensure all tests pass after completing tasks - write new tests when necessary
- Add comments only for non-obvious code logic
- Place imports at the top of files (in-function imports only to resolve circular dependencies)
